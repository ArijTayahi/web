{% extends 'base.html.twig' %}

{% block title %}Nouvelle Spécialité - Admin{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <h1 class="mb-4">
                <i class="fas fa-plus"></i> Créer une nouvelle spécialité
            </h1>

            <div class="card">
                <div class="card-body">
                    {{ form_start(form) }}
                        {{ form_row(form.nom) }}
                        {{ form_row(form.description) }}

                        <div class="d-flex justify-content-between mt-4">
                            <a href="{{ path('app_admin_specialite_index') }}" class="btn btn-secondary">
                                <i class="fas fa-arrow-left"></i> Retour
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save"></i> Créer la spécialité
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}