{% extends 'admin/base_admin.html.twig' %}

{% block title %}Appointments - MedTime Admin{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 fw-bold text-dark mb-0">Appointments</h1>
    <button class="btn btn-primary rounded-pill px-4" disabled>New Appointment</button>
</div>

<div class="card border-0 shadow-sm rounded-4">
    <div class="card-body">
        <div class="alert alert-info mb-0">
            No appointments are available yet. This section will list scheduled appointments once the module is enabled.
        </div>
    </div>
</div>
{% endblock %}
