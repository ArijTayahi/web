<div class="sidebar-brand mb-5">
	<div class="d-flex align-items-center gap-2">
		<div class="rounded-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px; background: var(--blue-pure);">
			<i class="fas fa-stethoscope text-white"></i>
		</div>
		<div>
			<h4 class="fw-bold mb-0" style="letter-spacing: -0.5px;">MedTime</h4>
			<small class="text-muted" style="letter-spacing: 1px; text-transform: uppercase; font-weight: 700;">Admin Panel</small>
		</div>
	</div>
</div>

<div class="mb-3 text-uppercase text-muted small fw-bold" style="letter-spacing: 1px;">Menu</div>
<a class="sidebar-link" href="{{ path('app_admin_dashboard') }}"><i class="fas fa-th-large me-2"></i>Overview</a>
<a class="sidebar-link" href="{{ path('app_admin_users') }}"><i class="fas fa-users me-2"></i>Users Management</a>
<a class="sidebar-link" href="{{ path('app_product_index') }}"><i class="fas fa-users me-2"></i>Produts Management</a>
<a class="sidebar-link" href="{{ path('app_product_category_index') }}"><i class="fas fa-users me-2"></i>Product Category</a>

<a class="sidebar-link" href="{{ path('app_admin_appointments') }}"><i class="fas fa-calendar-check me-2"></i>Appointments</a>
<a class="sidebar-link" href="{{ path('app_admin_doctor_verifications') }}"><i class="fas fa-shield-alt me-2"></i>Verifications</a>

<div class="mt-4 mb-3 text-uppercase text-muted small fw-bold" style="letter-spacing: 1px;">Settings</div>
<a class="sidebar-link" href="{{ path('app_admin_settings') }}"><i class="fas fa-cog me-2"></i>System Settings</a>

<div class="mt-4 p-3 rounded-4 border" style="background: #f8fbff; border-color: #e8eef7;">
	<div class="small text-muted mb-2">Logged in as</div>
	<div class="fw-bold text-dark">{{ app.user ? app.user.username : 'Admin' }}</div>
	<div class="d-flex align-items-center gap-2 mt-3">
		<a class="btn btn-sm btn-outline-primary w-100 rounded-pill" href="{{ path('app_profile_edit') }}">
			<i class="fas fa-user-edit me-1"></i>Edit
		</a>
		<a class="btn btn-sm btn-outline-danger w-100 rounded-pill" href="{{ path('app_logout') }}">
			<i class="fas fa-sign-out-alt me-1"></i>Logout
		</a>
	</div>
</div>
