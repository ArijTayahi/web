<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin Panel{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.5.2/css/all.min.css" rel="stylesheet">
    <link href="{{ asset('css/main.css') }}" rel="stylesheet">
    <style>
        :root {
            --blue-pure: #1d4ed8;
            --blue-700: #1e40af;
            --blue-500: #3b82f6;
            --blue-100: #eff6ff;
            --ghost-white: #f5f7ff;
            --text-navy: #0f172a;
            --text-muted: #64748b;
            --radius-xl: 32px;
            --radius-lg: 24px;
            --radius-md: 18px;
        }

        body { margin: 0; background: var(--ghost-white); color: var(--text-navy); font-family: 'Inter', system-ui, -apple-system, sans-serif; }
        .admin-layout { display: flex; min-height: 100vh; padding: 24px; gap: 24px; }

        .sidebar {
            width: 280px;
            background: #ffffff;
            color: var(--text-navy);
            padding: 28px 22px;
            border-radius: var(--radius-xl);
            border: 1px solid #e8eef7;
            box-shadow: 0 22px 40px -34px rgba(15, 23, 42, 0.35);
            position: sticky;
            top: 24px;
            height: calc(100vh - 48px);
        }

        .sidebar .sidebar-link {
            color: var(--text-muted);
            text-decoration: none;
            display: flex;
            align-items: center;
            padding: 12px 14px;
            border-radius: var(--radius-md);
            margin: 6px 0;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .sidebar .sidebar-link:hover { background: var(--blue-100); color: var(--blue-pure); }
        .sidebar .sidebar-link i { width: 20px; color: #94a3b8; }

        .content-area { flex: 1; display: flex; flex-direction: column; gap: 16px; }

        .admin-topbar {
            background: #ffffff;
            border: 1px solid #e8eef7;
            border-radius: var(--radius-lg);
            padding: 16px 22px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 20px 40px -36px rgba(15, 23, 42, 0.35);
        }

        .admin-search {
            display: flex;
            align-items: center;
            gap: 8px;
            background: #f8fbff;
            border: 1px solid #e5ecf7;
            padding: 8px 12px;
            border-radius: 999px;
            min-width: 320px;
        }

        .admin-search input { border: none; background: transparent; outline: none; width: 100%; }
        .admin-user { display: flex; align-items: center; gap: 10px; color: var(--text-navy); }
        .admin-avatar { width: 36px; height: 36px; background: var(--blue-pure); color: #fff; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: 700; }
        .content { padding: 24px 4px; }

        @media (max-width: 992px) {
            .admin-layout { padding: 0; }
            .sidebar { display: none; }
            .admin-search { min-width: 200px; }
            .admin-topbar { border-radius: 0; }
        }
    </style>
</head>
<body>
    <div class="admin-layout">
        <aside class="sidebar">
            {% include 'admin/_sidebar.html.twig' %}
        </aside>
        <div class="content-area">
            {% include 'admin/_navbar.html.twig' %}
            <main class="content">
                {% block content %}{% endblock %}
            </main>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
