{% extends 'base.html.twig' %}

{% block title %}Paiement consultation{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">💳 Paiement de la consultation #{{ consultation.id }}</h4>
        </div>

        <div class="card-body">
            <p><strong>Patient :</strong> {{ consultation.patient.username }}</p>
            <p><strong>Médecin :</strong> Dr. {{ consultation.medecin.username }}</p>
            <p><strong>Montant :</strong> <span class="badge bg-dark">50.00 DT</span></p>

            <hr>

            <form method="post">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('paiement_consultation_' ~ consultation.id) }}">

                <div class="mb-3">
                    <label class="form-label">Méthode de paiement</label>

                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="methode" id="edinar" value="EDINAR" checked>
                        <label class="form-check-label" for="edinar">🇹🇳 e-Dinar (Mock)</label>
                    </div>

                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="methode" id="mobile" value="MOBILE_MONEY">
                        <label class="form-check-label" for="mobile">📱 Mobile Money (Mock)</label>
                    </div>

                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="methode" id="sobflous" value="SOBFLOUS">
                        <label class="form-check-label" for="sobflous">💸 Sobflous (Mock)</label>
                    </div>
                </div>

                <div class="alert alert-info">
                    ℹ️ Paiement simulé : pour la soutenance tu expliques que l’API bancaire sera branchée plus tard.
                </div>

                <button type="submit" class="btn btn-success btn-lg">
                    ✅ Confirmer le paiement
                </button>

                <a href="{{ path('app_consultation_show', {id: consultation.id}) }}" class="btn btn-outline-secondary">
                    Annuler
                </a>
            </form>
        </div>
    </div>
</div>
{% endblock %}{% block javascripts %}
    <script src="{{ asset('assets/vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
{% endblock %}
