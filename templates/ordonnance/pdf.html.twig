<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Ordonnance {{ ordonnance.numeroOrdonnance }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
        }
        .header {
            text-align: center;
            border-bottom: 3px solid #28a745;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #28a745;
            margin: 0;
        }
        .info {
            margin-bottom: 30px;
        }
        .info p {
            margin: 5px 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        table th, table td {
            border: 1px solid #dee2e6;
            padding: 10px;
            text-align: left;
        }
        table th {
            background-color: #f8f9fa;
            font-weight: bold;
        }
        .instructions {
            margin-top: 30px;
            padding: 15px;
            background-color: #e7f3ff;
            border-left: 4px solid #0d6efd;
        }
        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 12px;
            color: #6c757d;
        }
        .qrcode {
            text-align: center;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🩺 ORDONNANCE MÉDICALE</h1>
        <p>{{ ordonnance.numeroOrdonnance }}</p>
    </div>

    <div class="info">
        <p><strong>Médecin :</strong> Dr. {{ ordonnance.consultation.medecin.username }}</p>
        <p><strong>Patient :</strong> {{ ordonnance.consultation.patient.username }}</p>
        <p><strong>Date d'émission :</strong> {{ ordonnance.dateEmission|date('d/m/Y') }}</p>
        {% if ordonnance.dateValidite %}
            <p><strong>Valide jusqu'au :</strong> {{ ordonnance.dateValidite|date('d/m/Y') }}</p>
        {% endif %}
    </div>

    {% if ordonnance.qrCode %}
        <div class="qrcode">
            <img src="{{ absolute_url(asset(ordonnance.qrCode)) }}" alt="QR Code" style="width: 150px;">
            <p style="font-size: 12px; color: #6c757d;">QR Code de vérification</p>
        </div>
    {% endif %}

    <h3>Médicaments prescrits</h3>
    <table>
        <thead>
            <tr>
                <th>Médicament</th>
                <th>Dosage</th>
                <th>Quantité</th>
                <th>Durée</th>
                <th>Instructions</th>
            </tr>
        </thead>
        <tbody>
            {% for ligne in ordonnance.lignes %}
                <tr>
                    <td>{{ ligne.nomMedicament }}</td>
                    <td>{{ ligne.dosage }}</td>
                    <td>{{ ligne.quantite }}</td>
                    <td>{{ ligne.dureeTraitement }}</td>
                    <td>{{ ligne.instructions ?? '-' }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    {% if ordonnance.instructions %}
        <div class="instructions">
            <strong>Instructions générales :</strong><br>
            {{ ordonnance.instructions }}
        </div>
    {% endif %}

    <div class="footer">
        <p>Ce document est une ordonnance médicale officielle</p>
        <p>MediSmart - Plateforme de télémédecine</p>
    </div>
</body>
</html>