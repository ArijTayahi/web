{% extends 'base.html.twig' %}

{% block title %}Modifier Ordonnance{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>Modifier l'Ordonnance</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Modifier') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_ordonnance_doctor_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
</div>
{% endblock %}
